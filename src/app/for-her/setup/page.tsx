import { GiftSetup } from "@/components/gift/gift-setup";

export const metadata = {
  title: "Setup · For Her",
  robots: { index: false, follow: false },
};

export default function SetupPage() {
  return <GiftSetup />;
}
